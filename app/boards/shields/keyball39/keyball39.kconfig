# MCU Configuration
CONFIG_SOC_SERIES_NRF52=y
CONFIG_SOC_NRF52840_QIAA=y

# Enable build optimizations similar to LTO in QMK
CONFIG_NO_OPTIMIZATIONS=n

# Build Options
CONFIG_BUILD_OUTPUT_HEX=y
CONFIG_BT_BOOTLOADER=y
